{
  "name": "@theludd/reader",
  "version": "2.0.0",
  "main": "dist/reader",
  "dependencies": {
    "@theludd/fantasy-functions": "^1.0.1",
    "fantasy-land": "^3.5.0"
  },
  "devDependencies": {
    "@theludd/eslint-config-gaius": "^1.0.4",
    "babel-cli": "^6.26.0",
    "babel-plugin-transform-builtin-extend": "^1.1.2",
    "babel-preset-env": "^1.6.1",
    "chai": "^4.1.2",
    "eslint": "^4.19.1",
    "mocha": "^4.0.1",
    "yafu": "^0.1.8"
  },
  "scripts": {
    "lint": "eslint lib test",
    "test": "mocha",
    "prepublish": "yarn test && yarn transpile",
    "transpile": "mkdir -p dist && babel lib -d dist"
  },
  "standard": {
    "env": "mocha"
  },
  "files": [
    "lib",
    "dist"
  ]
}
