{
  "name": "@theludd/reader",
  "version": "2.0.2",
  "main": "dist/reader",
  "dependencies": {
    "@theludd/fantasy-functions": "^1.0.1",
    "fantasy-land": "^3.5.0"
  },
  "devDependencies": {
    "@theludd/eslint-config-gaius": "^1.0.6",
    "babel-cli": "^6.26.0",
    "babel-plugin-transform-builtin-extend": "^1.1.2",
    "babel-preset-env": "^1.7.0",
    "chai": "^4.2.0",
    "eslint": "^5.6.1",
    "mocha": "^5.2.0",
    "yafu": "^0.1.9"
  },
  "scripts": {
    "lint": "eslint lib test",
    "test": "mocha",
    "prepublish": "yarn test && yarn transpile",
    "transpile": "mkdir -p dist && babel lib -d dist"
  },
  "standard": {
    "env": "mocha"
  },
  "files": [
    "lib",
    "dist"
  ]
}
